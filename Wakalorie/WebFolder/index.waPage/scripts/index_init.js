/**  * @fileOverview Code to init everything for the index page * @author Welsh Harris * @created 08/08/2013 * * @name Wakalorie * @copyright (c) 2013 CoreBits DataWorks LLC * @license Released under the MIT license (included in distribution in MIT LICENSE.txt) *//** @namespace */var WAKL = WAKL || {};//make sure the user has authorization to be hereWAKL.auth.loggedInCheck(WAKL.CONST.PAGE_LOGIN);//init everything on the pageWAF.onAfterInit = function() {    "use strict";        //get refs to our components    WAKL.foodGrid = $$("component1");    WAKL.foodAddModDlg = $$("component2");    WAKL.qtyAddArea = $$("component3");	WAKL.day = $$("component4");	WAKL.quickAddDlg = $$("component5");    WAKL.genFoodAddDlg = $$("component6");    WAKL.caloriesWeightsChart = $$("component7");    WAKL.header = $$("component8");    	//init components    WAKL.foodGrid.initC();    WAKL.foodAddModDlg.initC();    WAKL.qtyAddArea.initC();    WAKL.day.initC();	WAKL.quickAddDlg.initC();	WAKL.genFoodAddDlg.initC();	WAKL.caloriesWeightsChart.initC();	WAKL.header.initC();		//check to see if the user has logged in before, and if not	//display the help tips	if (WAKL.auth.isFirstLogin()) {		WAKL.tips.toggle();	}	 };